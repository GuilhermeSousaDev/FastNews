import React,{useState,useEffect} from 'react';

function DeputadosInfo(){
    const [data,setData] = useState([])
    useEffect(async () => {
        const request = fetch(``)
    })
    return(
        <div>
            
        </div>
    )
}

export default DeputadosInfo;